import { NextRequest,NextResponse } from "next/server";import { createServerSupabase } from "@/lib/supabase/server";import { createAdminSupabase } from "@/lib/supabase/admin";import { IssueInstitutionalRecordSchema,type HPSManifest } from "@/lib/hps/schema";import { verifyDetachedCanonical,signRegistryManifest } from "@/lib/hps/crypto";import { createHpsId } from "@/lib/hps/ids";
export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const s=await createServerSupabase();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:"Authentication required."},{status:401});const p=IssueInstitutionalRecordSchema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid institutional record.",details:p.error.issues},{status:400});const claim=p.data.institutionalClaim;if(claim.organizationId!==id)return NextResponse.json({error:"Organization mismatch."},{status:400});const a=createAdminSupabase();const [{data:member},{data:org},{data:key}]=await Promise.all([a.from("hps_org_members").select("role,status").eq("org_id",id).eq("user_id",user.id).single(),a.from("hps_organizations").select("*").eq("id",id).single(),a.from("hps_issuer_keys").select("*").eq("id",claim.issuerKeyId).eq("org_id",id).eq("status","active").single()]);if(!member||member.status!=="active"||!["admin","issuer"].includes(member.role))return NextResponse.json({error:"Authorized issuer role required."},{status:403});if(!org||org.verification_status!=="verified")return NextResponse.json({error:"Institution must be verified before it can issue HPS institutional records."},{status:403});if(!key||key.public_key!==claim.issuerPublicKey)return NextResponse.json({error:"Issuer key is not registered to this institution."},{status:400});if(!verifyDetachedCanonical(claim,p.data.institutionSignature,claim.issuerPublicKey))return NextResponse.json({error:"Institution signature verification failed."},{status:400});const publicKey=process.env.HPS_REGISTRY_PUBLIC_KEY,secretKey=process.env.HPS_REGISTRY_SECRET_KEY;if(!publicKey||!secretKey)return NextResponse.json({error:"Registry signing unavailable."},{status:503});let version=1;if(claim.parentRecordId){const {data:parent}=await a.from("hps_records").select("version,issuer_org_id").eq("id",claim.parentRecordId).single();if(!parent||parent.issuer_org_id!==id)return NextResponse.json({error:"Invalid parent record."},{status:400});version=(parent.version||1)+1}const recordId=createHpsId(),now=new Date().toISOString();const manifest:HPSManifest={hpsVersion:"1.0",id:recordId,work:{title:claim.title,type:claim.documentType,createdAt:claim.issuedAt,version,sha256:claim.assetHash,fileName:claim.fileName},actors:[{id:"issuer",name:org.name,role:"institutional_issuer",publicKey:claim.issuerPublicKey,identityAssurance:"authorized_issuer_verified",organizationId:id}],contributions:[],tools:[],evidence:[],responsibility:{finalApprovalActorId:"issuer",statement:`${org.name} issued this digital record through an authorized HPS institutional signer.`},issuedAt:now,institutionalClaim:claim,institutionSignature:{algorithm:"Ed25519",publicKey:claim.issuerPublicKey,keyId:claim.issuerKeyId,value:p.data.institutionSignature},interoperability:{c2pa:{mappingVersion:"1.0",status:"exportable"},verifiableCredential:{contextVersion:"2.0",status:"exportable"}}};const signed=signRegistryManifest(manifest,publicKey,secretKey);const {error}=await a.from("hps_records").insert({id:recordId,owner_user_id:user.id,title:claim.title,creator_name:org.name,work_type:claim.documentType,record_kind:"institutional_document",asset_hash:claim.assetHash,manifest:signed,registry_signature:signed.registrySignature!.value,registry_public_key:publicKey,institution_signature:p.data.institutionSignature,issuer_org_id:id,issuer_key_id:claim.issuerKeyId,version,parent_record_id:claim.parentRecordId||null,status:"active"});if(error)throw error;if(claim.parentRecordId)await a.from("hps_records").update({status:"superseded",superseded_by_id:recordId}).eq("id",claim.parentRecordId).eq("issuer_org_id",id);return NextResponse.json({id:recordId,manifest:signed},{status:201})}catch(e){console.error(e);return NextResponse.json({error:"Unable to issue institutional record."},{status:500})}}
